/**
 * 
 * 
 * @param {number} n - Amount of days
 * @param {number[]} arr - Prices per day
 * @param {number} k - Amount of money to spend
 */
function buyMaximumStocks (n, arr, k) {
  const money = k
  
  
  const total = arr.reduce((a, b) => a +=, 0)
}
