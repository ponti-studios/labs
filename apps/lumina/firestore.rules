rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // This app no longer uses Firestore for authentication or persistence.
    // Deny all access by default.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
